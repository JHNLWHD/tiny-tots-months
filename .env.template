# Tiny Tots Months - Environment Configuration Template
# Copy this file to .env and update with your actual values

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from your Supabase project dashboard
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# =============================================================================
# PAYMENT CONFIGURATION
# =============================================================================

# Supported currencies: "PHP", "USD", or "BOTH"
# PHP = Philippines Pesos only (manual payment methods)
# USD = US Dollars only (Stripe + PayPal)  
# BOTH = Both currencies with auto-detection and switcher
VITE_SUPPORTED_CURRENCIES=PHP

# -----------------------------------------------------------------------------
# PHP Payment Account Details (Required if PHP is supported)
# -----------------------------------------------------------------------------
# Update these with your actual payment account details
VITE_GCASH_NUMBER=0917-123-4567
VITE_BUSINESS_NAME="Tiny Tots Milestones"

# =============================================================================
# ANALYTICS & TRACKING (Optional)
# =============================================================================
# PostHog configuration (recommended for product analytics)
VITE_POSTHOG_API_KEY=your_posthog_project_key
VITE_POSTHOG_HOST=https://app.posthog.com

# =============================================================================
# FEATURE FLAGS (Optional)
# =============================================================================
# Enable/disable features for testing
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_PAYMENT_TRACKING=true
VITE_ENABLE_CREDIT_SYSTEM=true

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================
# Set to 'development' for local dev, 'production' for live
NODE_ENV=development

# Enable debug logging
VITE_DEBUG_MODE=false

# =============================================================================
# EXAMPLE CONFIGURATIONS
# =============================================================================

# Example 1: PHP Only (Current Setup)
# VITE_SUPPORTED_CURRENCIES=PHP
# VITE_GCASH_NUMBER=0917-123-4567

# Example 2: USD Only  
# VITE_SUPPORTED_CURRENCIES=USD
# VITE_STRIPE_PUBLISHABLE_KEY=pk_live_your_live_key

# Example 3: Both Currencies
# VITE_SUPPORTED_CURRENCIES=BOTH
# VITE_GCASH_NUMBER=0917-123-4567
# VITE_STRIPE_PUBLISHABLE_KEY=pk_live_your_live_key

# =============================================================================
# SECURITY NOTES
# =============================================================================
# - Never commit your actual .env file to version control
# - Use different keys for development and production
# - Rotate keys regularly for security
# - Keep your Supabase anon key secure but note it's safe for client-side use
# - Never expose your Supabase service role key in client-side code
